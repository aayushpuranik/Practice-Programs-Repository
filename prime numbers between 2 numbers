#include <stdio.h>
void prime(int,int);
int main()
{
   int s,e;
   scanf("%d",&s);
   scanf("%d",&e);
   prime(s,e);
}
void prime(int s,int e)
{
	int start,end;
	int j,count=0;
	start=s;
	end=e;
	while(start<=end)
	{
		j=2;
		count=0;
		while(j<=start/2)
		{
			if(start%j==0)
			{
				count=1;
				break;
			}
			j++;
		}
		if(count==0){ printf("%d\n",start);}
		start++;
	}
}
